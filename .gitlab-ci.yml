build-job:
  stage: build
  tags:
    - aws-ec2-docker
  script:
    - echo "Hello, $GITLAB_USER_LOGIN!"

test-job-docker-aws-ec2:
  stage: test
  tags:
    - aws-ec2-docker
  script:
    - echo "This job tests something"

# test-job-docker-mac:
#   stage: test
#   tags:
#     - yaroslav-mac
#   script:
#     - echo "It is run from docker@yaroslavs mac"

deploy-prod:
  stage: deploy
  tags:
    - aws-ec2-docker
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
  environment: production
