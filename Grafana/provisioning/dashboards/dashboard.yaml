apiVersion: 1
providers:
  - name: "Security Test Dashboard"  # 대시보드 이름
    orgId: 1
    folder: "Security Tests"
    type: file
    disableDeletion: false
    editable: true
    options:
      path: "/var/lib/grafana/dashboards/security-test-dashboard.json"  # JSON 대시보드 파일 경로

